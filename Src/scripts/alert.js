// test
const evens = [0, 2, 4, 6, 8];
const odds = evens.map(i => i + 1);

console.log(evens);
console.log(odds);

class Rectangle {
    constructor(height, width) {
        this.height = height;
        this.width = width;
    }
}